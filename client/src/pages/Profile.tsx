

const Profile = () => {
  return (
    <div className="min-h-screen bg-[rgb(var(--background))] text-[rgb(var(--text))] flex items-center justify-center">
        <div className="bg-[rgb(var(--card))] p-8 rounded-lg shadow-md w-full max-w-md">
            <h1 className="text-2xl font-bold mb-4">Profile</h1>
            <p className="text-lg">This is your profile page.</p>
        </div>
    </div>
  );
};

export default Profile;